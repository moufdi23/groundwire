supabase==2.31.0
python-dotenv==1.0.1
sentence-transformers==3.0.1
langchain-text-splitters==1.1.2
langchain-core==1.6.1        # shared by langchain-text-splitters + langgraph; >=1.4.7 required
tqdm>=4.67.0
beautifulsoup4==4.12.3
requests==2.32.3
rank-bm25==0.2.2

# Day 7: answer generation + Ragas-equivalent evaluation
anthropic                # Anthropic Python SDK — generate_answer.py + evaluate_ragas.py use Haiku directly

# Day 9: LangGraph agent
# Both install cleanly on Python 3.14 / Windows — no C-extension wheels, pure Python.
# No conflicts with the existing stack.
langgraph==1.2.11
langchain-anthropic==1.7.0

# ragas / langchain-anthropic / langchain-huggingface: CANNOT be installed on Python 3.14 / Windows.
#   ragas 0.4.3 depends on scikit-network (no py3.14 wheel; needs MSVC C++ build tools).
#   ragas 0.4.3 --no-deps hard-imports langchain_community.chat_models.vertexai, which was
#   removed in langchain-community >=0.3 — an irresolvable version conflict.
#   Resolution: evaluate_ragas.py implements the 4 metrics (Faithfulness, AnswerRelevancy,
#   ContextPrecision, ContextRecall) directly with Claude Haiku using the same math Ragas uses.
#   To try ragas on a system with MSVC: pip install ragas langchain-anthropic langchain-huggingface

# psycopg2-binary — needed only for direct DB connections (Day 2+).
# No pre-built wheel for Python 3.14 yet; install manually when ready:
#   pip install psycopg2-binary  (needs PostgreSQL headers, or install PostgreSQL first)
# Alternatively use:  pip install "psycopg[binary]"  (psycopg v3, has 3.14 wheels)
